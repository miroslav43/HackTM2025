"""
Parking system API routes.
TODO: Implement parking zones, vehicles, and session management.
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement parking endpoints
# - POST /parking/nearby (find nearby parking zones)
# - GET /parking/vehicles (get user vehicles)
# - POST /parking/start (start parking session)
# - GET /parking/active (get active parking sessions)
# - POST /parking/stop/{session_id} (stop parking session) 